<%= form_with model: @passenger, class: form_class do |f| %>

  <p><%= intro_text %></p>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :phone_num %>
  <%= f.text_field :phone_num %>

  <%= f.submit action_name, class:"passenger-form-btn"%>

  <% end%>